#include<iostream>
#include<cmath>
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
	    int p1, p2, k, res=0;
	    cin>>p1>>p2>>k;
	    res=floor((p1+p2)/k);
	    if(res%2==0) cout<<"CHEF"<<endl; // even.
	    else cout<<"COOK"<<endl;
	}
	return 0;
}
